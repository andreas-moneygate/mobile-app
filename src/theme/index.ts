import { DefaultTheme } from 'styled-components/native'
import { Spacings } from 'types/ui'

import lightThemeColors from './lightThemeColors'

export const lightTheme: DefaultTheme = {
  colors: lightThemeColors,
  spacings: Spacings,
  type: 'light',
}
